services:
  thehallmonitor:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist the SQLite database across container restarts
      - ./data:/app/data
    tmpfs:
      # abiword needs writable /tmp for scratch space during conversion
      - /tmp:size=128m
